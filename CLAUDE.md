# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## プロジェクト概要

このプロジェクトはチャットボット付きのホームページを作るプロジェクトです。

チャットボット付きのポートフォリオ／ホームページを構築するプロジェクト。
現在は `index.html` 単一ファイル構成（バニラ HTML / CSS / JS）。

## 言語・スタイルルール

- **返答はすべて日本語**で行う
- **コードのコメントは日本語**で記述する
- デザインは**モダンで洗練されたダークテーマ**を維持する（現行カラーパレットを参照）
- **レスポンシブ対応必須** — `@media (max-width: 480px)` を基準にモバイルレイアウトを確保する

## デザインシステム

現行の CSS カスタムプロパティ（変更する際はこれを基準にする）：

```css
--bg: #0a0a0f;          /* ページ背景 */
--surface: #12121a;     /* カード背景 */
--border: #1e1e2e;      /* ボーダー */
--accent: #7c3aed;      /* メインアクセント（紫） */
--accent-glow: #a78bfa; /* アクセントのハイライト */
--text: #e2e8f0;        /* 本文テキスト */
--muted: #64748b;       /* サブテキスト */
```

アンビエントグロー（`body::before / ::after`）は紫と青の2点で構成されており、
新しいセクションを追加する際もこの雰囲気を壊さないようにする。

## チャットボットの実装方針

チャットボットを追加する際の指針：

- UI は既存のカードデザインに合わせたダークスタイルで実装する
- チャットウィンドウは画面右下に固定（`position: fixed`）するか、専用セクションとして配置する
- バックエンド API を呼び出す場合は `fetch` を使い、エラー時はチャット内にインラインで日本語メッセージを表示する
- ストリーミングレスポンスを扱う場合は `ReadableStream` / SSE を使用する

## ファイル構成の方針

現状は単一 HTML ファイル。規模が大きくなる場合の分割方針：

```
index.html       # エントリーポイント
style.css        # グローバルスタイル（分離する場合）
chat.js          # チャットボットロジック（分離する場合）
```

CSS・JS を外部ファイルに分離する際は、`index.html` 内のインライン記述を削除してリンクに置き換える。
バンドラーは使わずバニラのままで進める（変更する場合はこのファイルを更新すること）。
